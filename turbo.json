{
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "globalEnv": [
    "__NEXT_IMAGE_OPTS",
    "NEXT_PUBLIC_ADOBE_SCRIPT",
    "NEXT_PUBLIC_IMAGE_REMOTE_HOSTNAME",
    "NODE_ENV",
    "CONTENT_API_BASE_URL",
    "AUTH_API_BASE_URL",
    "USER_API_BASE_URL",
    "ADBE_API_BASE_URL",
    "NEXTAUTH_URL"
  ],
  "tasks": {
    "build": {
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "dist/**",
        "generated/**"
      ],
      "dependsOn": [
        "^build"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": [
        "^build"
      ]
    },
    "lint": {
      "cache": false
    },
    "lint:fix": {
      "cache": false
    }
  }
}
