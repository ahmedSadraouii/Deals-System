.test:
  stage: Test
  image: node:18
  allow_failure: true
  artifacts:
    paths:
      - node_modules/
  before_script:
    - echo "Add some steps"
    #- npm install yarn
  script:
    - echo "Add some steps"
    #- yarn install
    #- yarn test
    #- yarn build

test_prod:
  extends:
    - .test
    - .environment_prod

test_dev:
  extends:
    - .test
    - .environment_dev

test_mr:
  extends:
    - .test
    - .environment_mr
